// Painter is a thin header-only wrapper around cairo_t*.
// This file exists solely so the CMake target has at least one .cpp in src/
// and to keep the build system pattern consistent across stages.

#include "painter.h"

namespace miniui {
// All Painter methods are defined inline in the header.
} // namespace miniui
