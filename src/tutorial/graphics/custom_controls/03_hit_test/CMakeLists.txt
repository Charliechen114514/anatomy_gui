cmake_minimum_required(VERSION 3.20)
add_executable(03_hit_test main.cpp)
target_include_directories(03_hit_test PRIVATE ${GRAPHICS_COMMON_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/../..)
target_link_libraries(03_hit_test PRIVATE user32 gdi32)
set_target_properties(03_hit_test PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
if(NOT MSVC)
    target_link_options(03_hit_test PRIVATE -municode)
endif()
