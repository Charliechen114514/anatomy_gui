cmake_minimum_required(VERSION 3.20)
add_executable(03_effects_layers main.cpp)
target_include_directories(03_effects_layers PRIVATE ${GRAPHICS_COMMON_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/../..)
target_link_libraries(03_effects_layers PRIVATE user32 d2d1 dwrite)
set_target_properties(03_effects_layers PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
if(NOT MSVC)
    target_link_options(03_effects_layers PRIVATE -municode)
endif()
