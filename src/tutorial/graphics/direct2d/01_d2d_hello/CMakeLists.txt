cmake_minimum_required(VERSION 3.20)
add_executable(01_d2d_hello main.cpp)
target_include_directories(01_d2d_hello PRIVATE ${GRAPHICS_COMMON_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/../..)
target_link_libraries(01_d2d_hello PRIVATE user32 d2d1)
set_target_properties(01_d2d_hello PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
if(NOT MSVC)
    target_link_options(01_d2d_hello PRIVATE -municode)
endif()
